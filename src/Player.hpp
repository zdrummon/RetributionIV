#pragma once

struct Player {
    int chunkX = 0;
    int chunkY = 0;
    int chunkZ = 0;
    int tileX = 0;
    int tileY = 0;
    // Add more properties as needed (e.g., facing, stats, etc.)
};